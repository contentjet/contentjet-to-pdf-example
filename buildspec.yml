version: 0.2
phases:
  install:
    commands:
      - apt-get install -Y libX11-dev
      - npm install
  pre_build:
    commands:
  build:
    commands:
      - node build.js
  post_build:
    commands:
artifacts:
  files:
    - output.pdf
